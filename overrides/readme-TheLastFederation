Needs to explicitly start the 32bit executable.

Game tries to save savegames and configuration in game dir, instead of home directory.
As part of configuration, set up some symlinks so saves and configuration are persistent.
